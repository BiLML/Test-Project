# --- FILE HỆ THỐNG & GIT ---
.git
.gitignore
.dockerignore
.DS_Store
Thumbs.db
.vscode/
.idea/
README.md

# --- MÔI TRƯỜNG & LOGS ---
# Docker sẽ tự cài môi trường mới, không copy môi trường cũ ở máy bạn vào
node_modules/
.venv/
venv/
env/
ENV/
__pycache__/
*.py[cod]
*.log
npm-debug.log*
yarn-debug.log*

# --- BUILD ARTIFACTS ---
build/
dist/

# --- BẢO MẬT ---
.env
.env.local
*.pem
*.key

# --- TỐI ƯU HÓA AI CHO DOCKER (QUAN TRỌNG) ---
# 1. KHÔNG copy thư mục backup model (vì Server chỉ chạy ONNX, không cần Keras)
backup_model_ai/

# 2. KHÔNG copy thư mục tools (vì Server không cần script convert)
tools/

# 3. KHÔNG copy file requirements.txt nếu nó nằm sai chỗ (nhưng thường ta cần nó)
# Lưu ý: Docker vẫn sẽ copy thư mục 'ai_onnx' vì ta KHÔNG liệt kê nó ở đây (Đúng ý đồ).